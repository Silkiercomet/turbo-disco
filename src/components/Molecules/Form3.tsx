import React from 'react'
import { FormsProps } from '../../hooks/useFormSession'

const Form3 = ({order, register, errors} : FormsProps) => {
  console.log(order)
  return (
    <div>Form3</div>
  )
}

export default Form3